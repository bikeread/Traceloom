# Invalid Fixture: Illegal Status Transition

This fixture is derived from the `user-tag-bulk-import` example and intentionally breaks a status-history transition.

Expected primary issue code:

- `invalid_status_transition`
