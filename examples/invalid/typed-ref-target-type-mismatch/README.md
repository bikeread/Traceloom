# Invalid Fixture: Typed Ref Target Type Mismatch

This fixture is derived from the `user-tag-bulk-import` example and intentionally breaks a header typed-ref target type.

Expected primary issue code:

- `typed_ref_target_type_mismatch`
