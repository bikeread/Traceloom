# Invalid Fixture: Supersedes Same Version

This fixture is derived from the committed versioned example and intentionally makes one artifact-level `supersedes` link connect two artifacts with the same version.

Expected primary issue code:

- `supersedes_same_version`
