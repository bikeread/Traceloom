# Invalid Fixture: Relation Endpoint Type Mismatch

This fixture is derived from the `user-tag-bulk-import` example and intentionally breaks a relation-edge endpoint type.

Expected primary issue code:

- `relation_endpoint_type_mismatch`
