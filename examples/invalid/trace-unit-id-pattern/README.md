# Invalid Fixture: Trace Unit ID Pattern

This fixture is derived from the `user-tag-bulk-import` example and intentionally breaks a trace-unit ID pattern.

Expected primary issue code:

- `invalid_pattern`
